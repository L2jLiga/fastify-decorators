/**
 * Enum used to define controller type
 */
export enum ControllerType {
  /**
   * Controller will use SINGLETON strategy
   */
  SINGLETON,

  /**
   * Controller will use REQUEST strategy
   */
  REQUEST,
}
