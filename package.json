{
  "name": "fastify-decorators",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node bin/start.js",
    "build": "yarn clean && yarn build:lib && yarn build:server",
    "build:server": "tsc",
    "clean": "yarn clean:server && yarn clean:lib",
    "clean:server": "rimraf dist/server",
    "clean:lib": "rimraf dist/fastify-decorators node_modules/fastify-decorators",
    "build:lib": "tsc -p ./lib/tsconfig.json && yarn cp",
    "cp": "yarn cp:assets && cp -r ./dist/fastify-decorators ./node_modules",
    "cp:assets": "cp ./lib/package.json ./dist/fastify-decorators && cp ./lib/README.md ./dist/fastify-decorators && cp ./LICENSE ./dist/fastify-decorators && cp -r ./lib/docs ./dist/fastify-decorators",
    "test": "yarn cp && tap test/*.test.ts --no-esm"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/L2jLiga/fastify-decorators.git"
  },
  "author": "Andrey Chalkin <L2jLiga@gmail.com> (https://github.com/L2jLiga)",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^12.12.17",
    "codecov": "^3.6.1",
    "fastify": "^2.11.0",
    "rimraf": "^3.0.0",
    "source-map-support": "^0.5.16",
    "tap": "^14.10.2",
    "typescript": "^3.7.3"
  },
  "engines": {
    "node": ">= 10.0.0",
    "yarn": "^1.0.0"
  }
}
